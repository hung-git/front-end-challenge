import React, { useState, useEffect } from 'react'

const ImageIndex = () => {
  const [images, setImages] = useState([]);

  const fetchImages = code;

  return (
    <div>
      list of images here
    </div>
  )
}

export default ImageIndex
